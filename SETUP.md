# セットアップ手順書

このアプリを手元で動かして、Asana のタスクをボード表示・チャット操作できるようにするまでの手順です。所要時間はおよそ 20〜30 分です。

## Asana は無償版でも使える?

**使えます。** Asana の MCP サーバ自体は無料で、無償プラン(Personal)のアカウントでも接続できます。ただし1点だけ制約があります:

| 機能 | 無償版 (Personal) | 有償版 (Starter 以上) |
|---|---|---|
| タスクの一覧取得(プロジェクト単位・担当者単位) | ✅ | ✅ |
| タスクの作成・更新・完了(議事録反映) | ✅ | ✅ |
| MCP サーバへの接続・OAuth | ✅ | ✅ |
| **高度なタスク検索**(横断検索 API) | ❌ | ✅ |

高度な検索が使えない分、無償版ではタスク取得が「プロジェクトをたどって一覧する」動きになり、プロジェクト数が多いと表示にやや時間がかかります。本アプリの Claude への指示には「検索が使えない場合は一覧取得で代替する」というルールを入れてあるので、無償版でもそのまま動きます。まず無償版で試して、チームで本格運用する段階で有償プランを検討すれば十分です。

## 事前に必要なもの

- **Node.js 20 以上**(https://nodejs.org/)
- **Anthropic アカウントと API キー** — Claude API は従量課金です(クレジットの事前購入が必要)
- **Asana アカウント** — 無償版で OK。試すだけなら新規アカウント+適当なワークスペースで十分

---

## Step 1. アプリの取得と依存関係のインストール

```bash
git clone https://github.com/okojyo1122/Claude_ToDoList.git
cd Claude_ToDoList
npm install
cp .env.example .env
```

## Step 2. Anthropic API キーの取得

1. https://console.anthropic.com/ にログイン(なければサインアップ)
2. **Billing** でクレジットを購入(最小額で OK。動作確認だけなら $5 程度で十分です)
3. **API Keys → Create Key** でキーを作成し、コピー
4. `.env` に貼り付け:

```
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxx
```

## Step 3. Asana で「MCP アプリ」を作成

Asana の MCP サーバ(`mcp.asana.com`)は OAuth 認証専用です。自分用の OAuth アプリを 1 つ登録します。

1. Asana にログインした状態で **開発者コンソール** を開く: https://app.asana.com/0/developer-console
2. **「新しいアプリを作成」(Create new app)** をクリック
3. アプリ名は任意(例: `タスク管理アプリ`)
4. アプリの種類を選ぶ画面では **必ず「MCP アプリ (MCP app)」を選択**
   - ⚠️ 通常の API アプリを選ぶと発行されるトークンが MCP サーバで使えません
5. 作成後、アプリの設定画面で:
   - **クライアント ID** と **クライアントシークレット** を控える
   - **リダイレクト URL** に次を登録する:
     ```
     http://localhost:8787/oauth/callback
     ```
6. `.env` に設定:

```
ASANA_CLIENT_ID=(控えたクライアントID)
ASANA_CLIENT_SECRET=(控えたクライアントシークレット)
```

## Step 4. Asana と連携(OAuth 認証)

```bash
npm run auth
```

1. ターミナルに URL が表示されるので、ブラウザで開く
2. Asana のログイン画面 → **ワークスペースを選択** → 「許可」
3. 「✅ Asana との連携が完了しました」と表示されたら成功

トークンは `config/.asana-token.json` に保存され、**期限切れは自動でリフレッシュ**されます(以後この手順は基本的に不要です)。

> 💡 ここで選んだワークスペースのタスクだけが操作対象になります。別のワークスペースに切り替えたいときは `npm run auth` を再実行してください。

## Step 5. 組織構成の設定

```bash
cp config/org.example.json config/org.json
```

`config/org.json` を自社(または試したい範囲)の構成に書き換えます。まず試すだけなら最小構成で OK:

```json
{
  "departments": [
    {
      "name": "開発部",
      "groups": [
        {
          "name": "プロダクトグループ",
          "teams": [
            {
              "name": "アプリチーム",
              "members": [
                { "name": "自分の名前", "asanaEmail": "自分のAsanaのメールアドレス" }
              ]
            }
          ]
        }
      ]
    }
  ]
}
```

`asanaEmail` は Asana アカウントのメールアドレスと一致させてください(担当者の特定に使われます)。

## Step 6. Asana 側にお試しデータを用意

動作確認用に、Asana のワークスペースに以下を作っておくと分かりやすいです:

1. プロジェクトを 1 つ作成(例: `営業案件A`)
2. タスクを 3〜4 件作成し、**自分を担当者**にして期限を設定
   - 期限を昨日にしたタスク(期限切れ表示の確認用)
   - 期限を今日にしたタスク
   - 期限を来週にしたタスク

## Step 7. 起動と動作確認

```bash
npm run dev
```

ブラウザで **http://localhost:3000** を開き、以下を順に試します:

| # | 操作 | 期待される動き |
|---|---|---|
| 1 | 「🔄 更新」を押す | Asana からタスクが読み込まれ、期限切れ/今日/予定の3列に表示。Claude が総評コメント |
| 2 | カードのチェックボックスを押す | Asana 上でタスクが完了になり、Claude が労ってくれる+お祝い演出 |
| 3 | チャットで「今日のタスク教えて」 | 割り当てタスクが整理されて表示される |
| 4 | 「📝 議事録を反映」→ 下のサンプルを貼って送信 | タスクの新規作成と完了が Asana に反映される |
| 5 | キャラクター横のセレクタで口調を変更 | 以後の応答の口調が変わる(ビジネス/フレンドリー/熱血コーチ) |

**議事録サンプル(コピーして使ってください):**

```
## 定例ミーティング議事録 (本日)

- A社への提案資料、山田さんが作成完了。レビュー済み。
- 新規: B社向けの見積もりを作成する(担当: 自分の名前、期限: 今週金曜)
- 新規: 次回定例のアジェンダ作成(担当: 自分の名前、期限: 明日)
```

→ 「作成完了」とあるタスクが Asana 上で完了になり、「新規」の2件がタスクとして追加されれば成功です。

---

## トラブルシューティング

| 症状 | 対処 |
|---|---|
| 「Asana の認証情報がありません」 | `npm run auth` を実行していない。Step 4 を実施 |
| 「トークンの取得に失敗しました (401)」 | クライアント ID / シークレットの写し間違い、またはアプリ種別が「MCP アプリ」でない。Step 3 を確認 |
| 認証後もツール呼び出しがエラーになる | リフレッシュトークンが失効した可能性。`npm run auth` を再実行 |
| タスク検索でエラーが出る・遅い | 無償プランの高度検索制約(冒頭参照)。動作はしますが、プロジェクト数が多いと遅くなります |
| 「ANTHROPIC_API_KEY が設定されていません」 | `.env` の設定を確認して再起動 |
| Claude の応答が返らない / 課金エラー | Anthropic Console のクレジット残高を確認 |
| ボードの読み込みが遅い | 仕様上、毎回 Claude が Asana を調べて整理するため数十秒かかることがあります |

## 料金の目安

- **Asana**: 無償版なら 0 円
- **Anthropic API**: 従量課金。このアプリは既定で `claude-opus-5` を使い、ボード更新やチャット 1 回あたり数円〜数十円程度が目安です(タスク数・議事録の長さで変動)。コストを抑えたい場合は `.env` で `CLAUDE_MODEL=claude-sonnet-5` に変更できます(精度と引き換えに単価が下がります)
